---
phase: 06-override-policy-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tools/skill_audit/policy.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_policy.py
  - tools/skill_audit/tests/test_ci_gating.py
autonomous: true
requirements:
  - RULE-01
  - RULE-02
user_setup: []
must_haves:
  truths:
    - Override precedence is deterministic: rule+tier > rule > tier > base default.
    - Overrides apply in default, CI, and changed-files modes.
    - Invalid override config returns runtime/config exit code `2`.
  artifacts:
    - path: tools/skill_audit/policy.py
      provides: override-aware severity resolution with deterministic precedence
      min_lines: 120
    - path: tools/skill_audit/cli.py
      provides: override config load/apply path and config-error handling in all modes
      min_lines: 320
    - path: tools/skill_audit/tests/test_policy.py
      provides: precedence and fallback behavior tests
      min_lines: 110
    - path: tools/skill_audit/tests/test_ci_gating.py
      provides: CI mode compatibility with override behavior
      min_lines: 170
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/policy.py
      via: parsed override profile is injected into policy translation for all scan modes
      pattern: override|policy|severity
---

<objective>
Integrate override policy resolution into runtime scan and gate pipeline with deterministic precedence and error handling.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-override-policy-profiles/06-CONTEXT.md
@.planning/phases/06-override-policy-profiles/06-RESEARCH.md
@.planning/phases/06-override-policy-profiles/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add override-aware policy resolution</name>
  <files>tools/skill_audit/policy.py</files>
  <action>Implement deterministic override precedence logic layered on top of base defaults and ensure severity remap behavior remains stable when no overrides are supplied.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/policy.py</automated></verify>
  <done>Policy module resolves severity using locked precedence contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire override loader into CLI runtime flow</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Load `.skill-audit-overrides.yaml` once per invocation, fail fast on invalid config with exit `2`, and apply overrides in default/CI/changed-files paths.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>Runtime pipeline consistently applies override policy across all modes.</done>
</task>

<task type="auto">
  <name>Task 3: Expand policy and CI compatibility tests</name>
  <files>tools/skill_audit/tests/test_policy.py, tools/skill_audit/tests/test_ci_gating.py</files>
  <action>Add precedence matrix tests and CI-mode behavior checks validating override compatibility and config-error returns.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_policy.py tools/skill_audit/tests/test_ci_gating.py -q</automated></verify>
  <done>Override precedence and CI integration are regression-protected.</done>
</task>

</tasks>

<verification>
- [ ] policy and CLI modules compile
- [ ] precedence behavior matches locked order
- [ ] invalid override config fails with exit `2`
- [ ] default/CI/changed-files modes apply overrides consistently
</verification>

<success_criteria>
- override policy resolution contract fully integrated and deterministic
- `RULE-01` and `RULE-02` runtime semantics satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-override-policy-profiles/06-02-SUMMARY.md`
</output>
