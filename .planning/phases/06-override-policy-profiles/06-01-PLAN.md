---
phase: 06-override-policy-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/override_config.py
  - tools/skill_audit/rules/__init__.py
  - tools/skill_audit/tests/test_override_config.py
autonomous: true
requirements:
  - RULE-01
  - RULE-02
user_setup: []
must_haves:
  truths:
    - Validator can load repository override config from `.skill-audit-overrides.yaml`.
    - Override config schema is strict and rejects unknown keys/types.
    - Unknown rule IDs are validated against current built-in rule registry.
  artifacts:
    - path: tools/skill_audit/override_config.py
      provides: strict YAML parser and typed override profile model
      min_lines: 140
    - path: tools/skill_audit/rules/__init__.py
      provides: canonical built-in rule ID registry export
      min_lines: 20
    - path: tools/skill_audit/tests/test_override_config.py
      provides: config schema and error-path coverage
      min_lines: 120
  key_links:
    - from: tools/skill_audit/override_config.py
      to: tools/skill_audit/rules/__init__.py
      via: parser validates override rule IDs against built-in registry
      pattern: rule|registry|validate
---

<objective>
Implement Phase 6 override config schema/parser module and canonical rule registry validation contract.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-override-policy-profiles/06-CONTEXT.md
@.planning/phases/06-override-policy-profiles/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strict override config parser module</name>
  <files>tools/skill_audit/override_config.py</files>
  <action>Add typed parser/validator for `.skill-audit-overrides.yaml` with strict key/type checks, severity enum validation, and deterministic parsed profile output.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/override_config.py</automated></verify>
  <done>Parser module exists and can load/validate repo override config with deterministic semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Export built-in rule registry for override validation</name>
  <files>tools/skill_audit/rules/__init__.py</files>
  <action>Expose canonical built-in rule ID set used by override parser to reject unknown rule IDs.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/rules/__init__.py</automated></verify>
  <done>Rule registry is available as source of truth for config validation.</done>
</task>

<task type="auto">
  <name>Task 3: Add override parser schema/error tests</name>
  <files>tools/skill_audit/tests/test_override_config.py</files>
  <action>Create tests for missing file behavior, malformed YAML, unknown keys, unknown rule IDs, invalid severities, and valid profile parsing.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_override_config.py -q</automated></verify>
  <done>Schema and error-path behavior are test-covered and deterministic.</done>
</task>

</tasks>

<verification>
- [ ] parser module compiles
- [ ] override parser test suite passes
- [ ] unknown keys/rules fail with config error semantics
- [ ] missing config file is non-error
</verification>

<success_criteria>
- `RULE-01` and schema validation portions of `RULE-02` are implemented and verified
</success_criteria>

<output>
After completion, create `.planning/phases/06-override-policy-profiles/06-01-SUMMARY.md`
</output>
