---
phase: 04-ci-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_ci_gating.py
  - tools/skill_audit/tests/test_output_options.py
autonomous: true
requirements:
  - CI-01
  - CI-02
user_setup: []
must_haves:
  truths:
    - CI mode supports threshold-based gating with explicit severity rank semantics.
    - CI default behavior fails on invalid findings while preserving non-CI behavior.
    - Exit-code contract distinguishes policy failure (1) from runtime/config failure (2).
  artifacts:
    - path: tools/skill_audit/cli.py
      provides: CI flag parsing, scope validation, threshold gate evaluation, compact CI output
      min_lines: 150
    - path: tools/skill_audit/tests/test_ci_gating.py
      provides: CI threshold/scope/exit-code matrix coverage
      min_lines: 120
    - path: tools/skill_audit/tests/test_output_options.py
      provides: coexistence assertions for CI flags with output options
      min_lines: 90
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/tests/test_ci_gating.py
      via: subprocess checks validate return code and CI output contract
      pattern: ci|max-severity|tiers|verbose-ci
---

<objective>
Implement CI policy gate controls and return-code semantics on the existing validator pipeline.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-gate-hardening/04-CONTEXT.md
@.planning/phases/04-ci-gate-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CI flags and config validation</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Add `--ci`, `--max-severity`, `--tiers`, and `--verbose-ci`; validate illegal combinations and malformed tier input with config-error exit code 2.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>CLI accepts Phase 4 CI arguments and rejects invalid configuration with explicit runtime/config failure semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement in-scope threshold gate and CI output modes</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Evaluate gate failures on in-scope findings using `--max-severity` rank model; emit compact CI output by default and full issue detail only with `--verbose-ci`; preserve non-CI behavior.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>CI mode returns 0/1/2 according to policy/config/runtime contract and prints deterministic gate summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add CI gate regression tests</name>
  <files>tools/skill_audit/tests/test_ci_gating.py, tools/skill_audit/tests/test_output_options.py</files>
  <action>Add subprocess tests for default CI threshold, strict mode, scoped warning-tolerant mode, invalid tier handling, verbose mode constraints, and output-flag coexistence.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_ci_gating.py -q</automated></verify>
  <done>CI gate contract is captured in stable tests and protected from regressions.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/cli.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_ci_gating.py -q passes
- [ ] default CI mode fails only on invalid findings
- [ ] non-CI mode behavior remains unchanged
</verification>

<success_criteria>
- CI-01 implemented with configurable threshold and split exit codes
- CI-02 implemented with explicit tier-scoped tolerant mode
- Subprocess regression tests validate policy and configuration behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-gate-hardening/04-01-SUMMARY.md`
</output>
