---
phase: 02-metadata-integrity-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/policy.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_policy.py
autonomous: true
requirements:
  - SCAN-03
user_setup: []
must_haves:
  truths:
    - Experimental-tier policy can downgrade approved metadata drift from invalid to warning without hiding true contract failures.
    - System and curated tiers retain strict invalid behavior for required contract violations.
    - Severity output remains deterministic after policy application.
  artifacts:
    - path: tools/skill_audit/policy.py
      provides: tier detection and severity-translation helpers
      min_lines: 60
    - path: tools/skill_audit/tests/test_policy.py
      provides: policy behavior tests across all tiers
      min_lines: 50
    - path: tools/skill_audit/cli.py
      provides: policy application in scan pipeline before reporting
      min_lines: 70
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/policy.py
      via: scan pipeline transforms findings using tier-aware policy
      pattern: apply|policy|tier
---

<objective>
Implement tier-policy resolution and severity override behavior for metadata findings.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metadata-integrity-rules/02-CONTEXT.md
@.planning/phases/02-metadata-integrity-rules/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier policy resolution module</name>
  <files>tools/skill_audit/policy.py</files>
  <action>Create helpers that derive tier from a finding path and apply severity policy transformations. Keep `.system` and `.curated` strict, and support warning-biased handling for approved `.experimental` cases.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/policy.py</automated></verify>
  <done>Policy helpers return stable translated severities per tier with no side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Wire policy into CLI scan flow</name>
  <files>tools/skill_audit/cli.py, tools/skill_audit/policy.py</files>
  <action>Integrate policy application in CLI after rule collection and before sorting/rendering so totals and outputs reflect tier-aware behavior consistently.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py tools/skill_audit/policy.py</automated></verify>
  <done>CLI reports translated severities while preserving deterministic ordering and rule IDs.</done>
</task>

<task type="auto">
  <name>Task 3: Add policy behavior tests</name>
  <files>tools/skill_audit/tests/test_policy.py, tools/skill_audit/policy.py</files>
  <action>Add pytest coverage for tier derivation and severity translation rules, including strict tiers and warning-biased experimental behavior.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_policy.py -q</automated></verify>
  <done>Tests prove SCAN-03 behavior and guard policy regressions.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/cli.py tools/skill_audit/policy.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_policy.py -q passes
- [ ] experimental policy translation appears in CLI output without affecting strict tiers
</verification>

<success_criteria>
- Tier policy behavior implemented and integrated
- SCAN-03 satisfied with deterministic output
- Tests guard policy behavior across tiers
</success_criteria>

<output>
After completion, create `.planning/phases/02-metadata-integrity-rules/02-01-SUMMARY.md`
</output>
