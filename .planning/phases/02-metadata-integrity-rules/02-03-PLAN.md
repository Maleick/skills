---
phase: 02-metadata-integrity-rules
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tools/skill_audit/rules/references.py
  - tools/skill_audit/rules/__init__.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_reference_rules.py
  - tools/skill_audit/tests/fixtures/references/
autonomous: true
requirements:
  - META-03
user_setup: []
must_haves:
  truths:
    - Broken local references declared by skill docs/metadata are detected with path-localized findings.
    - Web URLs are ignored by reference-path validation rules.
    - Reference findings remain read-only and actionable within the standard output pipeline.
  artifacts:
    - path: tools/skill_audit/rules/references.py
      provides: local reference extraction and validation logic
      min_lines: 90
    - path: tools/skill_audit/tests/test_reference_rules.py
      provides: reference validation coverage for missing paths and URL ignore behavior
      min_lines: 70
    - path: tools/skill_audit/cli.py
      provides: reference rule execution integration
      min_lines: 80
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/rules/references.py
      via: scan pipeline executes local reference checks per skill
    - from: tools/skill_audit/tests/test_reference_rules.py
      to: tools/skill_audit/rules/references.py
      via: fixture-based verification of extracted and missing paths
---

<objective>
Add local reference-path validation rules for skill metadata and docs.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metadata-integrity-rules/02-CONTEXT.md
@.planning/phases/02-metadata-integrity-rules/02-RESEARCH.md
@.planning/phases/02-metadata-integrity-rules/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement local reference validation rules</name>
  <files>tools/skill_audit/rules/references.py, tools/skill_audit/rules/__init__.py</files>
  <action>Implement extraction and validation for local skill references in docs/metadata, ignore URLs, resolve paths relative to each skill directory, and emit missing-path findings with remediation.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/rules/references.py</automated></verify>
  <done>Rule module detects unresolved local references and skips remote URL references.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reference checks into CLI</name>
  <files>tools/skill_audit/cli.py, tools/skill_audit/rules/references.py</files>
  <action>Integrate reference-path checks into the scan pipeline so each skill directory receives reference validation alongside existing metadata rules.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py tools/skill_audit/rules/references.py</automated></verify>
  <done>CLI output includes localized reference findings and consistent severity totals.</done>
</task>

<task type="auto">
  <name>Task 3: Add reference fixtures and regression tests</name>
  <files>tools/skill_audit/tests/fixtures/references/, tools/skill_audit/tests/test_reference_rules.py</files>
  <action>Create fixtures for valid local references, missing local references, and URL-only references, then add pytest coverage to lock expected behavior.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_reference_rules.py -q</automated></verify>
  <done>META-03 behavior is covered and deterministic for local-path validation.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/rules/references.py tools/skill_audit/cli.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_reference_rules.py -q passes
- [ ] broken local paths are reported while URL references are ignored
</verification>

<success_criteria>
- Local reference-path checks implemented and integrated
- META-03 requirement satisfied
- Tests verify missing-path and URL-ignore behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-metadata-integrity-rules/02-03-SUMMARY.md`
</output>
