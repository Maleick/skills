---
phase: 02-metadata-integrity-rules
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tools/skill_audit/rules/parity.py
  - tools/skill_audit/rules/__init__.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_parity_rules.py
  - tools/skill_audit/tests/fixtures/parity/
autonomous: true
requirements:
  - META-02
user_setup: []
must_haves:
  truths:
    - Skill metadata parity mismatches between `SKILL.md` and `agents/openai.yaml` are detected with one finding per mismatched field.
    - Missing counterpart metadata files are surfaced with actionable remediation.
    - Parity findings participate in the same policy-aware output pipeline.
  artifacts:
    - path: tools/skill_audit/rules/parity.py
      provides: cross-file metadata parity checks
      min_lines: 90
    - path: tools/skill_audit/tests/test_parity_rules.py
      provides: parity rule coverage for match, mismatch, and missing-file cases
      min_lines: 70
    - path: tools/skill_audit/cli.py
      provides: parity rule execution integration
      min_lines: 80
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/rules/parity.py
      via: scan pipeline executes parity checks per skill directory
    - from: tools/skill_audit/tests/test_parity_rules.py
      to: tools/skill_audit/rules/parity.py
      via: fixture-backed assertions on parity findings
---

<objective>
Implement `SKILL.md` to `agents/openai.yaml` metadata parity checks with actionable findings.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metadata-integrity-rules/02-CONTEXT.md
@.planning/phases/02-metadata-integrity-rules/02-RESEARCH.md
@.planning/phases/02-metadata-integrity-rules/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement parity rule module</name>
  <files>tools/skill_audit/rules/parity.py, tools/skill_audit/rules/__init__.py</files>
  <action>Create rule logic that reads `SKILL.md` frontmatter and `agents/openai.yaml`, compares mapped fields, and emits granular findings for mismatches and missing counterpart files.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/rules/parity.py</automated></verify>
  <done>Parity rule emits deterministic findings with clear path context and remediation hints.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate parity checks into CLI rule pipeline</name>
  <files>tools/skill_audit/cli.py, tools/skill_audit/rules/parity.py</files>
  <action>Run parity checks for each discovered skill and merge resulting findings with existing rules before policy translation and reporting.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py tools/skill_audit/rules/parity.py</automated></verify>
  <done>CLI output includes parity findings and severity totals reflect combined rule results.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity fixtures and tests</name>
  <files>tools/skill_audit/tests/fixtures/parity/, tools/skill_audit/tests/test_parity_rules.py</files>
  <action>Add fixture cases for matched metadata, mismatched metadata, and missing counterpart files, then cover each case with pytest assertions.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_parity_rules.py -q</automated></verify>
  <done>META-02 behavior is regression-tested and deterministic.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/rules/parity.py tools/skill_audit/cli.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_parity_rules.py -q passes
- [ ] CLI output includes actionable parity findings for mismatched or missing metadata counterparts
</verification>

<success_criteria>
- Parity checks implemented and wired into command flow
- META-02 requirement satisfied
- Fixtures and tests guard behavior across parity scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/02-metadata-integrity-rules/02-02-SUMMARY.md`
</output>
