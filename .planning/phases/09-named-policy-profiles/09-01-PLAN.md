---
phase: 09-named-policy-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/override_config.py
  - tools/skill_audit/tests/test_override_config.py
autonomous: true
requirements:
  - RULE-03
user_setup: []
must_haves:
  truths:
    - Override config supports named profile collections with strict schema validation.
    - Legacy single-profile config remains backward-compatible as implicit default profile.
    - Unknown profile names and ambiguous profile selection states fail fast with runtime/config errors.
  artifacts:
    - path: tools/skill_audit/override_config.py
      provides: named profile parser/selector contract with deterministic defaults and strict validation
      min_lines: 320
    - path: tools/skill_audit/tests/test_override_config.py
      provides: named profile schema + selection error matrix
      min_lines: 220
  key_links:
    - from: tools/skill_audit/override_config.py
      to: tools/skill_audit/tests/test_override_config.py
      via: selection and validation behavior for named/legacy override modes
      pattern: profiles|default_profile|selection|unknown
---

<objective>
Extend override configuration parsing to support named policy profiles with deterministic selection and strict fail-fast validation.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-named-policy-profiles/09-CONTEXT.md
@.planning/phases/09-named-policy-profiles/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add named-profile config model and deterministic selector</name>
  <files>tools/skill_audit/override_config.py</files>
  <action>Implement additive schema support for named profiles, deterministic active-profile selection rules, and backward-compatible legacy single-profile behavior while preserving strict validation semantics.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/override_config.py</automated></verify>
  <done>Override loader can resolve one active profile deterministically from named or legacy config modes.</done>
</task>

<task type="auto">
  <name>Task 2: Surface active profile identity in policy metadata contract</name>
  <files>tools/skill_audit/override_config.py</files>
  <action>Extend policy metadata helpers to include deterministic active profile identity and selection source in additive form for downstream output contracts.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/override_config.py</automated></verify>
  <done>Policy metadata includes stable active-profile fields without removing existing keys.</done>
</task>

<task type="auto">
  <name>Task 3: Add override parser regression tests for named profiles</name>
  <files>tools/skill_audit/tests/test_override_config.py</files>
  <action>Add tests for named profile parsing, default profile resolution, explicit profile selection, unknown profile failures, and ambiguous/mixed schema rejection.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_override_config.py -q</automated></verify>
  <done>Named-profile config contract and failure modes are regression-locked.</done>
</task>

</tasks>

<verification>
- [ ] named-profile parser compiles and preserves legacy compatibility
- [ ] deterministic profile selection behavior is implemented and strict
- [ ] override config regression tests pass
</verification>

<success_criteria>
- `RULE-03` profile schema and selection foundation is implemented with strict validation guarantees
</success_criteria>

<output>
After completion, create `.planning/phases/09-named-policy-profiles/09-01-SUMMARY.md`
</output>
