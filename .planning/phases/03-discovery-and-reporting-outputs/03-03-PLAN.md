---
phase: 03-discovery-and-reporting-outputs
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_output_options.py
  - tools/skill_audit/tests/fixtures/output/
autonomous: true
requirements:
  - INDEX-02
  - REPT-02
user_setup: []
must_haves:
  truths:
    - Default CLI behavior remains console-only when no output path flags are provided.
    - Explicit output paths fail on existing files unless force-overwrite is enabled.
    - Repeated runs over unchanged input produce deterministic JSON and markdown payloads.
  artifacts:
    - path: tools/skill_audit/tests/test_output_options.py
      provides: output routing and overwrite behavior regression tests
      min_lines: 80
    - path: tools/skill_audit/tests/fixtures/output/
      provides: deterministic output fixtures for repeated-run assertions
    - path: tools/skill_audit/cli.py
      provides: explicit file output/overwrite control flags
      min_lines: 110
  key_links:
    - from: tools/skill_audit/tests/test_output_options.py
      to: tools/skill_audit/cli.py
      via: CLI option behavior and file output assertions
---

<objective>
Finalize output behavior controls and deterministic regression coverage for Phase 3 outputs.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-discovery-and-reporting-outputs/03-CONTEXT.md
@.planning/phases/03-discovery-and-reporting-outputs/03-RESEARCH.md
@.planning/phases/03-discovery-and-reporting-outputs/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit output path and overwrite controls</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Implement `--json-out`, `--markdown-out`, and force-overwrite behavior so no-path runs stay console-only while explicit outputs respect fail-on-existing defaults.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>CLI enforces explicit output controls and overwrite policy exactly as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Add output behavior and determinism tests</name>
  <files>tools/skill_audit/tests/test_output_options.py, tools/skill_audit/tests/fixtures/output/</files>
  <action>Add tests for console-only defaults, output path writes, overwrite failures without force, overwrite success with force, and repeated-run deterministic JSON/markdown outputs.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_output_options.py -q</automated></verify>
  <done>Regression tests lock output routing, overwrite controls, and deterministic payload behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite validation and smoke run</name>
  <files>tools/skill_audit/cli.py, tools/skill_audit/tests/</files>
  <action>Run the full skill-audit test suite and smoke-run CLI output generation for both JSON and markdown to verify end-to-end integration.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests -q</automated></verify>
  <done>All existing and new tests pass; output contracts are stable and usable.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/cli.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_output_options.py -q passes
- [ ] python3 -m pytest tools/skill_audit/tests -q passes
- [ ] console default plus explicit output/overwrite behaviors verified by tests
</verification>

<success_criteria>
- Output behavior controls finalized
- Deterministic output regressions covered
- Phase 3 output contracts verified end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-and-reporting-outputs/03-03-SUMMARY.md`
</output>
