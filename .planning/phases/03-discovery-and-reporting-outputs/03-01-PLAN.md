---
phase: 03-discovery-and-reporting-outputs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/indexing.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_indexing.py
autonomous: true
requirements:
  - INDEX-01
  - INDEX-02
user_setup: []
must_haves:
  truths:
    - Maintainers can emit a machine-readable JSON skill index for all discovered skills.
    - Each skill record includes required core fields and worst-severity status.
    - Top-level JSON includes reliable per-tier and per-severity summary counts.
  artifacts:
    - path: tools/skill_audit/indexing.py
      provides: skill aggregation and JSON payload generation
      min_lines: 90
    - path: tools/skill_audit/tests/test_indexing.py
      provides: index schema and summary reconciliation tests
      min_lines: 70
    - path: tools/skill_audit/cli.py
      provides: JSON output integration and option routing
      min_lines: 90
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/indexing.py
      via: CLI builds JSON index payload from aggregated findings
      pattern: index|json
---

<objective>
Implement the JSON index and summary model from canonical findings.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-discovery-and-reporting-outputs/03-CONTEXT.md
@.planning/phases/03-discovery-and-reporting-outputs/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic skill index aggregation</name>
  <files>tools/skill_audit/indexing.py</files>
  <action>Implement per-skill aggregation from findings with required fields (`path`, `tier`, `status`, `finding_count`, `severity_counts`, key metadata fields) and top-level summary totals by tier and severity.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/indexing.py</automated></verify>
  <done>Index helper emits deterministic payloads and worst-severity status per skill.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate JSON index output into CLI</name>
  <files>tools/skill_audit/cli.py, tools/skill_audit/indexing.py</files>
  <action>Wire index generation into CLI so explicit JSON output paths can emit full index payload while preserving console-only defaults when no output path is provided.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py tools/skill_audit/indexing.py</automated></verify>
  <done>CLI can produce the JSON contract with stable ordering and summary blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Add JSON index tests</name>
  <files>tools/skill_audit/tests/test_indexing.py, tools/skill_audit/indexing.py</files>
  <action>Add pytest coverage for schema expectations, per-skill status derivation, and summary reconciliation against record-level totals.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_indexing.py -q</automated></verify>
  <done>INDEX-01 and INDEX-02 behavior is regression-tested and stable.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/indexing.py tools/skill_audit/cli.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_indexing.py -q passes
- [ ] JSON output includes per-skill records plus global/per-tier/per-severity summaries
</verification>

<success_criteria>
- JSON index contract implemented
- INDEX-01 and INDEX-02 satisfied
- Deterministic and reconciled summary counts verified
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-and-reporting-outputs/03-01-SUMMARY.md`
</output>
