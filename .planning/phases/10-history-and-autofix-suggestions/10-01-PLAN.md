---
phase: 10-history-and-autofix-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/history.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_history.py
  - tools/skill_audit/tests/test_output_options.py
autonomous: true
requirements:
  - HIST-01
user_setup: []
must_haves:
  truths:
    - CLI can emit deterministic history snapshot artifacts from scan results.
    - Snapshot output remains opt-in and follows existing overwrite safety behavior.
    - Snapshot payload includes deterministic global, per-tier, and per-severity trend-ready aggregates.
  artifacts:
    - path: tools/skill_audit/history.py
      provides: snapshot schema builder/serializer and baseline loader utilities
      min_lines: 180
    - path: tools/skill_audit/cli.py
      provides: snapshot flag plumbing integrated with existing output controls
      min_lines: 620
    - path: tools/skill_audit/tests/test_history.py
      provides: snapshot determinism and overwrite safety regression matrix
      min_lines: 180
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/history.py
      via: build and write deterministic history snapshots from existing index payloads
      pattern: history|snapshot|write
    - from: tools/skill_audit/tests/test_history.py
      to: tools/skill_audit/history.py
      via: deterministic payload and error-path assertions
      pattern: deterministic|overwrite|baseline
---

<objective>
Implement deterministic history snapshot schema and persistence pipeline as an additive opt-in feature.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-history-and-autofix-suggestions/10-CONTEXT.md
@.planning/phases/10-history-and-autofix-suggestions/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic history snapshot model and IO helpers</name>
  <files>tools/skill_audit/history.py</files>
  <action>Create a dedicated history module that builds deterministic snapshot payloads from scan/index data, validates schema version, and safely reads/writes snapshot files.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/history.py</automated></verify>
  <done>Snapshot model and IO helpers exist with deterministic ordering and strict error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire snapshot output controls into CLI without breaking defaults</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Add explicit history output arguments and route resolved scan/index payload through snapshot generation only when requested, preserving existing default output behavior.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>CLI can emit snapshot artifact on demand while unchanged invocations keep prior behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add snapshot determinism and overwrite-safety tests</name>
  <files>tools/skill_audit/tests/test_history.py, tools/skill_audit/tests/test_output_options.py</files>
  <action>Add regression tests for deterministic repeated snapshots, explicit overwrite behavior, and coexistence with existing output flags.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_history.py tools/skill_audit/tests/test_output_options.py -q</automated></verify>
  <done>Snapshot contract and output safety behavior are regression-locked.</done>
</task>

</tasks>

<verification>
- [ ] history module compiles with deterministic schema + IO helpers
- [ ] CLI supports opt-in snapshot output without default behavior regressions
- [ ] snapshot determinism and overwrite tests pass
</verification>

<success_criteria>
- `HIST-01` snapshot artifact foundation is implemented and deterministic
</success_criteria>

<output>
After completion, create `.planning/phases/10-history-and-autofix-suggestions/10-01-SUMMARY.md`
</output>
