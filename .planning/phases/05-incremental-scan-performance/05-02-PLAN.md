---
phase: 05-incremental-scan-performance
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tools/skill_audit/cli.py
  - tools/skill_audit/indexing.py
  - tools/skill_audit/reporting.py
  - tools/skill_audit/markdown_report.py
  - tools/skill_audit/tests/test_output_options.py
  - tools/skill_audit/tests/test_findings_reporting.py
autonomous: true
requirements:
  - PERF-02
  - PERF-03
user_setup: []
must_haves:
  truths:
    - Compare-range input controls changed-file scope deterministically.
    - Incremental runs emit explicit scope and scanned-count metadata in JSON and human-readable outputs.
    - Invalid compare-range or git-scope failures return runtime/config error code 2.
  artifacts:
    - path: tools/skill_audit/cli.py
      provides: compare-range flag parsing, error handling, and metadata assembly
      min_lines: 250
    - path: tools/skill_audit/indexing.py
      provides: scan metadata inclusion in JSON index payload
      min_lines: 170
    - path: tools/skill_audit/reporting.py
      provides: console report metadata rendering
      min_lines: 70
    - path: tools/skill_audit/markdown_report.py
      provides: markdown summary metadata rendering
      min_lines: 70
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/indexing.py
      via: CLI-provided scope metadata is serialized into index payload
      pattern: compare-range|scan|scope
---

<objective>
Add compare-range controls and deterministic incremental metadata output across JSON, console, and markdown channels.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-incremental-scan-performance/05-RESEARCH.md
@.planning/phases/05-incremental-scan-performance/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compare-range controls and validation</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Add explicit compare-range CLI control for changed-files discovery and enforce fail-fast runtime/config errors when range resolution fails.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>Maintainers can provide a compare range to scope incremental runs with deterministic behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Thread scan metadata into output renderers</name>
  <files>tools/skill_audit/indexing.py, tools/skill_audit/reporting.py, tools/skill_audit/markdown_report.py</files>
  <action>Include mode, compare range, changed-file count, impacted-skill count, and scanned counts in output payloads and rendered summaries without introducing non-deterministic fields.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/indexing.py tools/skill_audit/reporting.py tools/skill_audit/markdown_report.py</automated></verify>
  <done>All output channels clearly communicate active scope and scan counts for incremental runs.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI/output tests for compare range and metadata</name>
  <files>tools/skill_audit/tests/test_output_options.py, tools/skill_audit/tests/test_findings_reporting.py</files>
  <action>Add subprocess and renderer tests to lock compare-range behavior, metadata presence, and stable output formatting.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_output_options.py tools/skill_audit/tests/test_findings_reporting.py -q</automated></verify>
  <done>Compare-range controls and incremental output metadata are regression-protected.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile on modified modules succeeds
- [ ] compare-range path accepts valid ranges and rejects invalid ranges with exit 2
- [ ] JSON/console/markdown outputs include scope and scanned-count metadata
- [ ] output remains deterministic for unchanged input
</verification>

<success_criteria>
- `PERF-02` and `PERF-03` behavior implemented across CLI and outputs
- operator-facing metadata clarifies incremental scan scope and counts
</success_criteria>

<output>
After completion, create `.planning/phases/05-incremental-scan-performance/05-02-SUMMARY.md`
</output>
