---
phase: 05-incremental-scan-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/scanner.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_scanner.py
autonomous: true
requirements:
  - PERF-01
user_setup: []
must_haves:
  truths:
    - Changed-files mode scans only impacted skill directories.
    - Impacted-skill filtering is deterministic and tier-safe.
    - Full-scan behavior remains unchanged when changed-files mode is not requested.
  artifacts:
    - path: tools/skill_audit/scanner.py
      provides: changed-file discovery and impacted-skill filtering helpers
      min_lines: 100
    - path: tools/skill_audit/cli.py
      provides: changed-files mode selection and scanner integration
      min_lines: 220
    - path: tools/skill_audit/tests/test_scanner.py
      provides: deterministic scope-mapping tests for incremental scan mode
      min_lines: 80
  key_links:
    - from: tools/skill_audit/scanner.py
      to: tools/skill_audit/cli.py
      via: CLI requests scoped skill directories through scanner helpers
      pattern: changed-files|impacted|discover
---

<objective>
Implement changed-files scope collection and normalized impacted-skill filtering so Phase 5 can run incremental scans safely.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-incremental-scan-performance/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic impacted-skill filtering helpers</name>
  <files>tools/skill_audit/scanner.py</files>
  <action>Add helpers to map repo-relative changed file paths into canonical skill directory keys and filter discovered skill directories to only impacted skills in deterministic order.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/scanner.py</automated></verify>
  <done>Scanner can deterministically compute incremental skill scope from changed file paths.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate changed-files scan mode in CLI execution flow</name>
  <files>tools/skill_audit/cli.py</files>
  <action>Add changed-files mode argument and wire scanner filtering so validator executes rules only on impacted skills when incremental mode is selected.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>CLI supports incremental scan path while keeping full-scan default unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add scanner-level scope correctness tests</name>
  <files>tools/skill_audit/tests/test_scanner.py</files>
  <action>Add tests for changed-file to skill mapping, deterministic output ordering, and no-impact cases.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_scanner.py -q</automated></verify>
  <done>Incremental skill-scope logic is covered by deterministic unit tests.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/scanner.py tools/skill_audit/cli.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_scanner.py -q passes
- [ ] changed-files mode excludes unaffected skills
- [ ] full-scan mode behavior remains unchanged by default
</verification>

<success_criteria>
- `PERF-01` implemented and validated
- deterministic impacted-skill filtering added without breaking full-scan mode
</success_criteria>

<output>
After completion, create `.planning/phases/05-incremental-scan-performance/05-01-SUMMARY.md`
</output>
