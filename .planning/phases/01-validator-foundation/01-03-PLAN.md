---
phase: 01-validator-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - tools/skill_audit/rules/skill_md.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/fixtures/skills_valid/sample-skill/SKILL.md
  - tools/skill_audit/tests/fixtures/skills_invalid/missing-frontmatter/SKILL.md
  - tools/skill_audit/tests/fixtures/skills_invalid/missing-file/
  - tools/skill_audit/tests/test_skill_md_rules.py
autonomous: true
requirements:
  - META-01
user_setup: []
must_haves:
  truths:
    - Missing `SKILL.md` files are detected and reported as findings.
    - Malformed `SKILL.md` frontmatter is detected and reported with remediation guidance.
    - Validation remains read-only with no file mutation actions.
  artifacts:
    - path: tools/skill_audit/rules/skill_md.py
      provides: SKILL.md existence and frontmatter validation rules
      min_lines: 70
    - path: tools/skill_audit/tests/test_skill_md_rules.py
      provides: rule behavior coverage for valid and invalid cases
      min_lines: 60
    - path: tools/skill_audit/cli.py
      provides: rule execution integration in baseline scan command
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/rules/skill_md.py
      via: scan pipeline executes skill_md rules for each skill directory
    - from: tools/skill_audit/tests/test_skill_md_rules.py
      to: tools/skill_audit/rules/skill_md.py
      via: fixture-based test coverage of rule outputs
---

<objective>
Complete Phase 1 with `SKILL.md` metadata validation.

Purpose: Deliver META-01 while honoring the read-only and narrow-scope constraints.
Output: SKILL.md rule module, fixtures, tests, and CLI integration.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validator-foundation/01-CONTEXT.md
@.planning/phases/01-validator-foundation/01-RESEARCH.md
@.planning/phases/01-validator-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SKILL.md presence and frontmatter rules</name>
  <files>tools/skill_audit/rules/skill_md.py, tools/skill_audit/cli.py</files>
  <action>Implement rules that detect missing `SKILL.md`, missing YAML frontmatter, invalid frontmatter mapping shape, and missing required keys (`name`, `description`). Route all rule outcomes through the existing findings model with actionable remediation text.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/rules/skill_md.py</automated></verify>
  <done>Rule module emits deterministic findings for missing/malformed `SKILL.md` cases and uses shared finding format.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixtures for valid and invalid skill metadata states</name>
  <files>tools/skill_audit/tests/fixtures/skills_valid/sample-skill/SKILL.md, tools/skill_audit/tests/fixtures/skills_invalid/missing-frontmatter/SKILL.md, tools/skill_audit/tests/fixtures/skills_invalid/missing-file/</files>
  <action>Create test fixtures that represent valid skill metadata, missing frontmatter, and missing file cases. Keep fixtures minimal and explicit so test intent is clear.</action>
  <verify><automated>python3 -c "from pathlib import Path; assert Path('tools/skill_audit/tests/fixtures/skills_valid/sample-skill/SKILL.md').exists()"</automated></verify>
  <done>Fixture set covers valid and invalid metadata states needed for rule verification.</done>
</task>

<task type="auto">
  <name>Task 3: Add metadata rule tests and finalize CLI integration</name>
  <files>tools/skill_audit/tests/test_skill_md_rules.py, tools/skill_audit/cli.py, tools/skill_audit/rules/skill_md.py</files>
  <action>Add pytest coverage that asserts findings for each invalid fixture and no invalid findings for valid fixtures. Ensure CLI includes these rule checks in baseline command output without introducing write/fix behavior.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_skill_md_rules.py -q</automated></verify>
  <done>META-01 behavior is covered by automated tests and exposed through the baseline validator command.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/rules/skill_md.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_skill_md_rules.py -q passes
- [ ] baseline command reports missing/malformed SKILL.md findings with remediation guidance
</verification>

<success_criteria>
- All tasks completed
- SKILL.md missing/malformed validation fully implemented
- Phase 1 remains read-only and scoped to baseline validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-validator-foundation/01-03-SUMMARY.md`
</output>
