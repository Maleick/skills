---
phase: 01-validator-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tools/skill_audit/findings.py
  - tools/skill_audit/reporting.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/tests/test_findings_reporting.py
autonomous: true
requirements:
  - SCAN-02
user_setup: []
must_haves:
  truths:
    - Findings expose severity (`valid|warning|invalid`), rule ID, path, and remediation guidance.
    - Maintainer receives clear severity totals after a scan run.
  artifacts:
    - path: tools/skill_audit/findings.py
      provides: Typed findings model and severity constraints
      min_lines: 50
    - path: tools/skill_audit/reporting.py
      provides: Severity summary aggregation and output rendering
      min_lines: 50
    - path: tools/skill_audit/tests/test_findings_reporting.py
      provides: Model and summary behavior tests
      min_lines: 40
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/findings.py
      via: CLI builds findings using shared model
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/reporting.py
      via: CLI renders severity totals from reporting helper
---

<objective>
Add the baseline findings contract and summary behavior.

Purpose: Satisfy SCAN-02 with structured issue semantics and actionable output.
Output: Findings model, reporting utilities, and tests wired into CLI.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validator-foundation/01-CONTEXT.md
@.planning/phases/01-validator-foundation/01-RESEARCH.md
@.planning/phases/01-validator-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typed findings model</name>
  <files>tools/skill_audit/findings.py, tools/skill_audit/cli.py</files>
  <action>Create a typed findings structure with required fields (`id`, `severity`, `path`, `message`, `suggested_fix`) and enforce the three allowed severity values. Update CLI flow to use the model for all emitted results.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/findings.py</automated></verify>
  <done>CLI-generated findings are normalized and include required fields with valid severity values.</done>
</task>

<task type="auto">
  <name>Task 2: Add severity summary reporting helpers</name>
  <files>tools/skill_audit/reporting.py, tools/skill_audit/cli.py</files>
  <action>Implement reporting helpers to aggregate finding totals by severity and print a concise actionable summary at the end of command execution. Preserve deterministic ordering in report output.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/reporting.py</automated></verify>
  <done>Command output includes consistent severity totals and clear remediation-oriented messages.</done>
</task>

<task type="auto">
  <name>Task 3: Test findings and summary behavior</name>
  <files>tools/skill_audit/tests/test_findings_reporting.py, tools/skill_audit/findings.py, tools/skill_audit/reporting.py</files>
  <action>Add pytest coverage for severity validation, required finding fields, and summary aggregation output. Include failure-case tests for invalid severities to prevent regressions.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_findings_reporting.py -q</automated></verify>
  <done>Tests validate findings model invariants and summary aggregation behavior.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/findings.py tools/skill_audit/reporting.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_findings_reporting.py -q passes
- [ ] scan output shows severity totals and actionable issue entries
</verification>

<success_criteria>
- All tasks completed
- Structured findings contract implemented and enforced
- Severity summary output available in command flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-validator-foundation/01-02-SUMMARY.md`
</output>
