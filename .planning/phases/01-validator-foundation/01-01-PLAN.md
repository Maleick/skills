---
phase: 01-validator-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_audit/__init__.py
  - tools/skill_audit/cli.py
  - tools/skill_audit/scanner.py
  - tools/skill_audit/tests/test_scanner.py
autonomous: true
requirements:
  - SCAN-01
user_setup: []
must_haves:
  truths:
    - Maintainer can run one command to scan `.system`, `.curated`, and `.experimental` in one pass.
    - Scan output uses deterministic ordering for skill directories.
  artifacts:
    - path: tools/skill_audit/cli.py
      provides: CLI entrypoint for full-tier scan command
      min_lines: 40
    - path: tools/skill_audit/scanner.py
      provides: Tier-aware deterministic skill directory discovery
      min_lines: 60
    - path: tools/skill_audit/tests/test_scanner.py
      provides: Scanner coverage tests for all tiers
      min_lines: 40
  key_links:
    - from: tools/skill_audit/cli.py
      to: tools/skill_audit/scanner.py
      via: CLI command invokes scanner function
      pattern: scan|discover
---

<objective>
Establish the Phase 1 command and scanner foundation.

Purpose: Deliver the one-command baseline scan path across all skill tiers.
Output: Initial CLI entrypoint, deterministic scanner module, and scanner tests.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validator-foundation/01-CONTEXT.md
@.planning/phases/01-validator-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill audit CLI package skeleton</name>
  <files>tools/skill_audit/__init__.py, tools/skill_audit/cli.py</files>
  <action>Create a Python package under `tools/skill_audit` with a CLI entrypoint that accepts a repository root argument and runs a baseline scan command. Keep implementation read-only and avoid any write/fix code paths in this phase.</action>
  <verify><automated>python3 -m py_compile tools/skill_audit/cli.py</automated></verify>
  <done>CLI module imports and executes without syntax errors; baseline scan command path exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic tier scanner</name>
  <files>tools/skill_audit/scanner.py, tools/skill_audit/cli.py</files>
  <action>Implement scanner logic that enumerates directories only under `skills/.system`, `skills/.curated`, and `skills/.experimental`, then returns sorted deterministic paths. Wire CLI command to call this scanner and print simple inventory output.</action>
  <verify><automated>python3 -c "from tools.skill_audit.scanner import discover_skill_dirs; import pathlib; print(len(discover_skill_dirs(pathlib.Path('.'))))"</automated></verify>
  <done>Running the command traverses all three tiers in one pass with stable sorted output.</done>
</task>

<task type="auto">
  <name>Task 3: Add scanner tests for tier coverage</name>
  <files>tools/skill_audit/tests/test_scanner.py, tools/skill_audit/scanner.py</files>
  <action>Add pytest coverage that validates scanner inclusion of all three tiers and deterministic ordering behavior. Use fixture-style temporary paths and avoid coupling tests to machine-specific absolute paths.</action>
  <verify><automated>python3 -m pytest tools/skill_audit/tests/test_scanner.py -q</automated></verify>
  <done>Tests prove the scanner includes expected tiers and preserves deterministic ordering.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile tools/skill_audit/cli.py tools/skill_audit/scanner.py succeeds
- [ ] python3 -m pytest tools/skill_audit/tests/test_scanner.py -q passes
- [ ] scan command discovers `.system`, `.curated`, `.experimental` in one run
</verification>

<success_criteria>
- All tasks completed
- One-command scan path implemented for all tiers
- Deterministic scanner behavior verified by tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-validator-foundation/01-01-SUMMARY.md`
</output>
